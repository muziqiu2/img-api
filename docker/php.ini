; 魔法师随机图片API - PHP 运行时配置覆盖
; memory_limit 256M：足够本应用与更新系统解压/打包
; max_execution_time 300：与 updater.php 的 UPDATE_TIMEOUT 对齐
memory_limit = 256M
max_execution_time = 300
upload_max_filesize = 20M
post_max_size = 20M
max_input_vars = 3000
